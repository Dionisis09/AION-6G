# Results Guide

Experiment artifacts are stored under the results directory as timestamped JSON files. Each artifact contains the parsed intent, candidate evaluation, selected target, execution summary, verification status, and fallback information.
